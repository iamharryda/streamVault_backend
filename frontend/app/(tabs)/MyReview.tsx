import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

export default function MyReview() {
  return (
    <SafeAreaView>
      <Text>My Movie Reviews</Text>
    </SafeAreaView>
  );
}